import { NebOptionsFilterPipe } from './neb-options-filter.pipe';

describe('NebOptionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NebOptionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
