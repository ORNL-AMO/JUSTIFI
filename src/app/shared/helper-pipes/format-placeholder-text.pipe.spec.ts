import { FormatPlaceholderTextPipe } from './format-placeholder-text.pipe';

describe('FormatPlaceholderTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlaceholderTextPipe();
    expect(pipe).toBeTruthy();
  });
});
