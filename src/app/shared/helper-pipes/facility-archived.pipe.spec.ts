import { FacilityArchivedPipe } from './facility-archived.pipe';

describe('FacilityArchivedPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityArchivedPipe();
    expect(pipe).toBeTruthy();
  });
});
