import { ReportTypeDisplayPipe } from './report-type-display.pipe';

describe('ReportTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
