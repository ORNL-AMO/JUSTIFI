import { FilterArchivedContactsPipe } from './filter-archived-contacts.pipe';

describe('FilterArchivedContactsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArchivedContactsPipe();
    expect(pipe).toBeTruthy();
  });
});
