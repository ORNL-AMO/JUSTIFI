import { AssessmentItemPipe } from './assessment-item.pipe';

describe('AssessmentItemPipe', () => {
  it('create an instance', () => {
    const pipe = new AssessmentItemPipe();
    expect(pipe).toBeTruthy();
  });
});
