import { IncludesEnergyEquipmentPipe } from './includes-energy-equipment.pipe';

describe('IncludesEnergyEquipmentPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludesEnergyEquipmentPipe();
    expect(pipe).toBeTruthy();
  });
});
