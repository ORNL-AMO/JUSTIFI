import { IncludesProcessEquipmentPipe } from './includes-process-equipment.pipe';

describe('IncludesProcessEquipmentPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludesProcessEquipmentPipe();
    expect(pipe).toBeTruthy();
  });
});
