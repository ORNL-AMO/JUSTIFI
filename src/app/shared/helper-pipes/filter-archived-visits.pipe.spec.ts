
import { FilterArchivedVisitsPipe } from './filter-archived-visits.pipe';

describe('FilterArchivedVisitsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArchivedVisitsPipe();
    expect(pipe).toBeTruthy();
  });
});
