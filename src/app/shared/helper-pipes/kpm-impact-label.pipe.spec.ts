import { KpmImpactLabelPipe } from './kpm-impact-label.pipe';

describe('KpmImpactLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new KpmImpactLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
