import { ProcessEquipmentListPipe } from './process-equipment-list.pipe';

describe('ProcessEquipmentListPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessEquipmentListPipe();
    expect(pipe).toBeTruthy();
  });
});
