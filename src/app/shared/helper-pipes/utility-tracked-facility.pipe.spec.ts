import { UtilityTrackedFacilityPipe } from './utility-tracked-facility.pipe';

describe('TrackedUtilityFacilityPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilityTrackedFacilityPipe();
    expect(pipe).toBeTruthy();
  });
});
