import { FacilityKpiListPipe } from './facility-kpi-list.pipe';

describe('FacilityKpiListPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityKpiListPipe();
    expect(pipe).toBeTruthy();
  });
});
