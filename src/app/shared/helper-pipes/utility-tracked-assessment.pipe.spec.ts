import { UtilityTrackedAssessmentPipe } from "./utility-tracked-assessment.pipe";

describe('TrackedUtilityAssessmentPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilityTrackedAssessmentPipe();
    expect(pipe).toBeTruthy();
  });
});
