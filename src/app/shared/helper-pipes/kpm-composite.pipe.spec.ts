import { KpmCompositePipe } from './kpm-composite.pipe';

describe('KpmCompositePipe', () => {
  it('create an instance', () => {
    const pipe = new KpmCompositePipe();
    expect(pipe).toBeTruthy();
  });
});
