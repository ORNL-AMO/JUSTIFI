import { ContactInvalidPipe } from './contact-invalid.pipe';

describe('ContactInvalidPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactInvalidPipe();
    expect(pipe).toBeTruthy();
  });
});
