import { IncludedInAssessmentPipe } from './included-in-assessment.pipe';

describe('IncludedInAssessmentPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludedInAssessmentPipe();
    expect(pipe).toBeTruthy();
  });
});
