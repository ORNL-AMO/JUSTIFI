import { DisplayRoundedValuesPipe } from './display-rounded-values.pipe';

describe('DisplayRoundedValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayRoundedValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
