import { NebTotalSavingsPipe } from './neb-total-savings.pipe';

describe('NebTotalSavingsPipe', () => {
  it('create an instance', () => {
    const pipe = new NebTotalSavingsPipe();
    expect(pipe).toBeTruthy();
  });
});
