import { IsUtilityTrackedPipe } from './is-utility-tracked.pipe';

describe('IsUtilityTrackedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsUtilityTrackedPipe();
    expect(pipe).toBeTruthy();
  });
});
