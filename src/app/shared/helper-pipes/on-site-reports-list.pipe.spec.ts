import { OnSiteReportsListPipe } from './on-site-reports-list.pipe';

describe('OnSiteReportsListPipe', () => {
  it('create an instance', () => {
    const pipe = new OnSiteReportsListPipe();
    expect(pipe).toBeTruthy();
  });
});
