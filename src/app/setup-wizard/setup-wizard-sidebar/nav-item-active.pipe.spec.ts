import { NavItemActivePipe } from './nav-item-active.pipe';

describe('NavItemActivePipe', () => {
  it('create an instance', () => {
    const pipe = new NavItemActivePipe();
    expect(pipe).toBeTruthy();
  });
});
